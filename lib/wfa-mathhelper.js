//angle i radianer.
//returnerer grader.
function toDegrees (angle) {
  return angle * (180 / Math.PI);
}

//angle i grader.
//returnerer radianer.
function toRadians (angle) {
  return angle * (Math.PI / 180);
}